function crcTable(){const t=new Uint32Array(256);for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=(c&1)?(0xedb88320^(c>>>1)):(c>>>1);t[n]=c>>>0;}return t;}const CRC=crcTable();
function crc32(bytes){let c=0xffffffff;for(const b of bytes)c=CRC[(c^b)&255]^(c>>>8);return(c^0xffffffff)>>>0;}
function u16(n){return new Uint8Array([n&255,(n>>>8)&255]);}
function u32(n){return new Uint8Array([n&255,(n>>>8)&255,(n>>>16)&255,(n>>>24)&255]);}
function concat(parts){let len=0;for(const p of parts)len+=p.length;const out=new Uint8Array(len);let o=0;for(const p of parts){out.set(p,o);o+=p.length;}return out;}
function safeName(name,index){const cleaned=String(name||`photo-${index+1}`).replace(/[\\/:*?"<>|]/g,"-").trim();return cleaned||`photo-${index+1}`;}
export async function buildBrowserZip(files,onProgress){const enc=new TextEncoder(),locals=[],centrals=[];let offset=0;for(let i=0;i<files.length;i++){onProgress?.(i,files.length,"download");const response=await fetch(files[i].url,{mode:"cors",cache:"no-store"});if(!response.ok)throw new Error(`Téléchargement impossible (${response.status}).`);const data=new Uint8Array(await response.arrayBuffer());const name=enc.encode(safeName(files[i].name,i));const crc=crc32(data);const local=concat([u32(0x04034b50),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),name,data]);locals.push(local);const central=concat([u32(0x02014b50),u16(20),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]);centrals.push(central);offset+=local.length;onProgress?.(i+1,files.length,"download");}const centralSize=centrals.reduce((n,p)=>n+p.length,0);const end=concat([u32(0x06054b50),u16(0),u16(0),u16(files.length),u16(files.length),u32(centralSize),u32(offset),u16(0)]);return new Blob([...locals,...centrals,end],{type:"application/zip"});}
