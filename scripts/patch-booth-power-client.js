const fs=require("fs");
const path=require("path");
let changes=0;

const serverPath=path.join(process.cwd(),"server.js");
let server=fs.readFileSync(serverPath,"utf8");

const oldDefault='function defaultBoothControl(){return {display:{enabled:false,locked:false,opacity:80},command:null};}';
const newDefault='function defaultBoothControl(){return {display:{enabled:false,locked:false,opacity:80},powerClient:{enabled:false,powerAt:null,visible:false,locked:false,opacity:90,updatedAt:null},command:null};}';
if(server.includes(oldDefault)){server=server.replace(oldDefault,newDefault);changes++;}

const oldRead='try{const p=JSON.parse(row.value||"{}");return {display:{enabled:Boolean(p?.display?.enabled),locked:Boolean(p?.display?.locked),opacity:Math.max(20,Math.min(100,Number(p?.display?.opacity)||80))},command:p?.command||null};}catch{return defaultBoothControl()}';
const newRead='try{const p=JSON.parse(row.value||"{}");return {display:{enabled:Boolean(p?.display?.enabled),locked:Boolean(p?.display?.locked),opacity:Math.max(20,Math.min(100,Number(p?.display?.opacity)||80))},powerClient:{enabled:Boolean(p?.powerClient?.enabled),powerAt:p?.powerClient?.powerAt||null,visible:Boolean(p?.powerClient?.visible),locked:Boolean(p?.powerClient?.locked),opacity:Math.max(25,Math.min(100,Number(p?.powerClient?.opacity)||90)),updatedAt:p?.powerClient?.updatedAt||null},command:p?.command||null};}catch{return defaultBoothControl()}';
if(server.includes(oldRead)){server=server.replace(oldRead,newRead);changes++;}

const oldCommand='app.post("/api/admin/booths/:boothName/command",adminOnly,async(req,res)=>{const boothName=String(req.params.boothName||"").trim().toUpperCase();const command=String(req.body?.command||"").trim().toUpperCase();if(!["LOLA","NINA","GABIN"].includes(boothName))return res.status(400).json({ok:false,message:"Borne invalide."});if(!["RESTART","SHUTDOWN"].includes(command))return res.status(400).json({ok:false,message:"Commande invalide."});const control=await readBoothControl(boothName);control.command={id:crypto.randomUUID(),type:command,status:"PENDING",createdAt:new Date().toISOString()};await writeBoothControl(boothName,control);res.json({ok:true,command:control.command});});';
const newCommand='app.post("/api/admin/booths/:boothName/command",adminOnly,async(req,res)=>{const boothName=String(req.params.boothName||"").trim().toUpperCase();const command=String(req.body?.command||"").trim().toUpperCase();if(!["LOLA","NINA","GABIN"].includes(boothName))return res.status(400).json({ok:false,message:"Borne invalide."});if(!["RESTART","SHUTDOWN","POWER_SCHEDULE","POWER_SHOW","POWER_HIDE","POWER_SETTINGS"].includes(command))return res.status(400).json({ok:false,message:"Commande invalide."});const control=await readBoothControl(boothName);control.powerClient=control.powerClient||{enabled:false,powerAt:null,visible:false,locked:false,opacity:90,updatedAt:null};let powerAt=null;if(command==="POWER_SCHEDULE"){const parsed=new Date(String(req.body?.powerAt||""));if(Number.isNaN(parsed.getTime()))return res.status(400).json({ok:false,message:"Date/heure Power invalide."});powerAt=parsed.toISOString();control.powerClient={...control.powerClient,enabled:true,powerAt,visible:false,updatedAt:new Date().toISOString()};}else if(command==="POWER_SHOW"){control.powerClient={...control.powerClient,enabled:true,visible:true,updatedAt:new Date().toISOString()};}else if(command==="POWER_HIDE"){control.powerClient={...control.powerClient,enabled:false,powerAt:null,visible:false,updatedAt:new Date().toISOString()};}else if(command==="POWER_SETTINGS"){if(req.body?.locked!==undefined)control.powerClient.locked=Boolean(req.body.locked);if(req.body?.opacity!==undefined)control.powerClient.opacity=Math.max(25,Math.min(100,Number(req.body.opacity)||90));control.powerClient.updatedAt=new Date().toISOString();}control.command={id:crypto.randomUUID(),type:command,status:"PENDING",createdAt:new Date().toISOString(),...(powerAt?{powerAt}:{})};await writeBoothControl(boothName,control);res.json({ok:true,command:control.command,powerClient:control.powerClient||null});});';
if(server.includes(oldCommand)){server=server.replace(oldCommand,newCommand);changes++;}

const oldConfigured='return {...s,display:controls[name].display,lastCommand:controls[name].command};';
const newConfigured='return {...s,display:controls[name].display,powerClient:controls[name].powerClient,lastCommand:controls[name].command};';
if(server.includes(oldConfigured)){server=server.replace(oldConfigured,newConfigured);changes++;}
fs.writeFileSync(serverPath,server,"utf8");

const appPath=path.join(process.cwd(),"client","src","App.jsx");
let app=fs.readFileSync(appPath,"utf8");

if(!app.includes('async function powerClientCommand(name,command,powerAt=null,settings=null)')){
  const anchor='  const frDate=v=>v?new Date(v).toLocaleDateString("fr-FR"):"—";';
  const helpers=`  async function powerClientCommand(name,command,powerAt=null,settings=null){\n    try{\n      const body={command,...(powerAt?{powerAt}:{}),...(settings||{})};\n      const r=await fetch(\`/api/admin/booths/\${encodeURIComponent(name)}/command\`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});\n      const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||"Commande Power impossible.");\n      await load();\n      return d;\n    }catch(e){alert(e.message||"Commande Power impossible.");return null}\n  }\n  function schedulePowerClient(name){const el=document.getElementById(\`lp28-power-\${name}\`);if(!el?.value)return alert("Choisis une date et une heure.");const d=new Date(el.value);if(Number.isNaN(d.getTime()))return alert("Date/heure invalide.");powerClientCommand(name,"POWER_SCHEDULE",d.toISOString())}\n  const powerLocalValue=v=>{if(!v)return "";const d=new Date(v);if(Number.isNaN(d.getTime()))return "";const z=n=>String(n).padStart(2,"0");return \`\${d.getFullYear()}-\${z(d.getMonth()+1)}-\${z(d.getDate())}T\${z(d.getHours())}:\${z(d.getMinutes())}\`};\n`;
  if(!app.includes(anchor))throw new Error('[LP28] Ancre helpers AdminBooths introuvable.');
  app=app.replace(anchor,anchor+'\n'+helpers);changes++;
}else{
  // Older generated UI used window.location.reload(), which returns the SPA to Home.
  const oldReload='      if(command!=="POWER_SETTINGS")window.location.reload();';
  if(app.includes(oldReload)){app=app.replace(oldReload,'      await load();\n      return d;');changes++;}
}

if(!app.includes('className="lp28-power-client-admin"')){
  const anchor='<button className="ghost" style={{marginTop:10}} onClick={()=>openHistory(b.boothName)}>🧾 Historique imprimante</button>';
  const ui=`<button className="ghost" style={{marginTop:10}} onClick={()=>openHistory(b.boothName)}>🧾 Historique imprimante</button>\n          <div className="lp28-power-client-admin" style={{marginTop:10,padding:12,border:"1px solid #5b4b22",borderRadius:12}}>\n            <div style={{display:"flex",justifyContent:"space-between",gap:8,alignItems:"center",flexWrap:"wrap"}}><strong style={{fontSize:18,color:"#f5d45c"}}>⏻ Arrêt client</strong><span className="muted">{b.powerClient?.visible?"🟡 Bouton affiché":b.powerClient?.enabled?"🟢 Programmé":"⚪ Désactivé"}</span></div>\n            {b.powerClient?.powerAt&&<div className="muted" style={{marginTop:5}}>Disponible à partir du {new Date(b.powerClient.powerAt).toLocaleString("fr-FR",{dateStyle:"short",timeStyle:"short"})}</div>}\n            <div style={{display:"flex",gap:7,flexWrap:"wrap",marginTop:9,alignItems:"center"}}>\n              <input id={\`lp28-power-\${b.boothName}\`} type="datetime-local" defaultValue={powerLocalValue(b.powerClient?.powerAt)} style={{minWidth:190}}/>\n              <button className="primary" onClick={()=>schedulePowerClient(b.boothName)}>💾 Programmer</button>\n              <button className="ghost" onClick={()=>powerClientCommand(b.boothName,"POWER_SHOW")}>👁 Afficher maintenant</button>\n              <button className="ghost" onClick={()=>powerClientCommand(b.boothName,"POWER_HIDE")}>🚫 Masquer</button>\n            </div>\n            <div className="muted" style={{marginTop:6,fontSize:12}}>Aucun arrêt automatique : le client choisit quand éteindre la borne.</div>\n            <div style={{marginTop:12,padding:10,border:"1px solid #5b4b22",borderRadius:10}}>\n              <label style={{display:"flex",gap:9,alignItems:"center",fontWeight:700}}><input type="checkbox" checked={Boolean(b.powerClient?.visible)} onChange={e=>powerClientCommand(b.boothName,e.target.checked?"POWER_SHOW":"POWER_HIDE")}/> ⏻ Afficher le bouton Power sur la borne</label>\n              <label style={{display:"flex",gap:9,alignItems:"center",fontWeight:700,marginTop:10}}><input type="checkbox" checked={Boolean(b.powerClient?.locked)} onChange={e=>powerClientCommand(b.boothName,"POWER_SETTINGS",null,{locked:e.target.checked})}/> 🔒 Verrouiller la position <b style={{marginLeft:"auto"}}>{b.powerClient?.locked?"OUI":"NON"}</b></label>\n              <div style={{marginTop:10,fontWeight:700}}>✨ Transparence : {b.powerClient?.opacity??90} %</div>\n              <input type="range" min="25" max="100" step="5" defaultValue={b.powerClient?.opacity??90} style={{width:"100%"}} onChange={e=>powerClientCommand(b.boothName,"POWER_SETTINGS",null,{opacity:Number(e.target.value)})}/>\n              <div className="muted" style={{marginTop:7,fontSize:12}}>Déverrouillé : déplacez le bouton directement sur l’écran de la borne, puis verrouillez sa position ici. La position est mémorisée par l’Agent.</div>\n            </div>\n          </div>`;
  if(!app.includes(anchor))throw new Error('[LP28] Bouton historique imprimante introuvable après patch précédent.');
  app=app.replace(anchor,ui);changes++;
}

for(const v of ['Version 8.5.91','Version 8.5.90','Version 8.5.89','Version 8.5.88'])if(app.includes(v))app=app.split(v).join('Version 8.5.92');
fs.writeFileSync(appPath,app,"utf8");
console.log(`[LP28] patch-booth-power-client : ${changes} modification(s) appliquée(s).`);
